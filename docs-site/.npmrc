# Don't re-run `pnpm install` (and fail on the build-scripts gate) before every
# `pnpm build` / `pnpm dev`. esbuild ships its platform binary as an optional
# dependency, so its postinstall is not required for Vite to work.
verify-deps-before-run=false
