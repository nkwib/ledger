# Cloudflare Workers + Static Assets configuration for the @blocco/ledger docs site.
# Deploy after `pnpm build` produces ./build:
#   pnpm build && npx wrangler deploy

name = "ledger-docs"
compatibility_date = "2026-05-04"

[assets]
directory = "./build"
not_found_handling = "404-page"
